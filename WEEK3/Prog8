#include <stdio.h>

int main() {
    int n, length = 0;
    printf("Enter a number: ");
    scanf("%d", &n);

    while (n != 0) {
        n /= 10;
        length++;
    }
    printf("Length of the number: %d\n", length);
    return 0;
}
