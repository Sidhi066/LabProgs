#include <stdio.h>
#include <math.h>

int main() {
    float num, squareRoot;

    printf("Enter a number: ");
    scanf("%f", &num);

    squareRoot = sqrt(num);
    printf("Square root: %.2f\n", squareRoot);

    return 0;
}
